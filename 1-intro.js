const amount = 12

if (amount < 10) {
    console.log("\nSmall number");
} else {
    console.log("\nLarge number");
}
/* 
short if syntax
console.log(amount < 10 ? "small" : "large");
*/

console.log(`\they its my first node app. Amout is -> ${amount} \n\n`);